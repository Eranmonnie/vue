import {defineStore} from "pinia"
export const countstore =defineStore("duplicate",{
// like data in options
    state:()=>{

    },
// like computed in options 
    getters:{

    },
// like methods in options
    actions:{

    }
})
// we impoturt it into the document we need instantiate it and thata it 